import palette from './../colors'

// ⚛️ MuiBottomNavigation
export default {
  root: {
    position: `absolute`,
    bottom: 0,
    width: `100%`,
    backgroundColor: palette.secondary[500],
  },
}
